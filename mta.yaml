_schema-version: "3.2"
ID: com-incture-customPortal
description: Custom Portal
version: 0.0.1
modules:
- name: com-incture-customPortal-router
  type: approuter.nodejs
  path: router
  requires:
  - name: com-incture-customPortal-html5-repo-runtime
  - name: com-incture-customPortal-uaa
  - name: com-incture-customPortal-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  - name: com-incture-customPortal-connectivity
  parameters:
    disk-quota: 256M
    memory: 256M
- name: com-incture-customPortal-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: com-incture-customPortal-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comincturecustomportal.zip
      name: comincturecustomportal
      target-path: resources/
- name: comincturecustomportal
  type: html5
  path: customportal
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: com-incture-customPortal-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: com-incture-customPortal
    service: xsuaa
    service-plan: application
- name: com-incture-customPortal-html5-repo-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: com-incture-customPortal-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: com-incture-customPortal-destination-service
    service-plan: lite
- name: com-incture-customPortal-connectivity
  type: org.cloudfoundry.managed-service
  parameters:
    service: connectivity
    service-plan: lite
- name: com-incture-customPortal-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: com-incture-customPortal-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
